language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t egbeoso/docker-react -f Dockerfile.dev .


script:
    - docker run  -e CI=true egbeoso/docker-react npm run test 